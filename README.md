# regobjsautomation
This Project uses the exposed XML api to automate bulk registry operations such as domain create, delete, renew and view. These Project also supports setting and sending alerts to the domain owners contacts in case of any major warnings or domain renewal alerts. These automatic alerts let the domain owners to renew there domain on time.
